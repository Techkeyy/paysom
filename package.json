{
  "name": "reactpay-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "mkdir -p src/app src/components src/lib && cp page.tsx src/app/ && cp layout.tsx src/app/ && cp globals.css src/app/ && cp providers.tsx src/app/ && cp App.tsx src/components/ && cp chain.ts src/lib/ && cp contracts.ts src/lib/ && next build --no-lint",
    "start": "next start"
  },
  "dependencies": {
    "next": "14.2.5",
    "react": "^18",
    "react-dom": "^18",
    "ethers": "^6.13.0",
    "viem": "^2.17.0",
    "wagmi": "^2.12.0",
    "@wagmi/core": "^2.13.0",
    "@tanstack/react-query": "^5.51.1"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18"
  }
}
